# 2026-09-24

## 分析：repos/ 下哪个是「广告学2班」的教学仓库

**结论**：`repos/ads-stats-2025`（真身 `C:\Users\yangjh\Desktop\ads-stats-2025`，远端 `yangjh-xbmu/ads-stats-2025`）。

**判定依据**

1. 直接证据：`repos/ads-stats-2025/README.md` 首行「2025 级广告学 2 班《统计与数据分析》课程演示仓库」。
2. 推送规律（宏哥给的解码器：1 班周三晚上 / 2 班周四下午）：
   - ads-stats-2025：4 次提交全在 2026-09-17（周四）17:44–17:50 → 落在周四下午课时段尾；无周三提交。
   - statistics-data-analysis：最后推送 2026-09-16（周三）21:10–21:24 → 周三晚上（≈19:30–21:10 节次收尾）；按同一规律应属 1 班。
   - 交叉印证：bigdata-ai-assignments 提交全在周四上午 09:37–11:21（09-03 / 09-17 / 09-24 三个周四）→ 「提交时间跟着当天课时段走」这个规律成立。
3. 旁证：`agent-skills/skills/random-roll-call/data/2026-spring/term.toml` 里确实用 `[[schedule]] weekday/start/end` 建模课表（该校 08:40 / 10:40 / 19:30 三档起始时间），说明"按星期+时段"编码课程是本人在用的口径。

**已披露的口径冲突（不静默取舍）**

- `statistics-data-analysis` 在 2026-09-03（周四）17:27–18:26 有 5 次提交，同样落在周四下午/傍晚，与「它=1 班」推断冲突。可解释为建仓日（同日 11:13 还建了 bigdata-ai-assignments、17:34 建了 agent-skills），但无法从仓库内证伪。
- `repos/ads-stats-2025` 无 `.git`，是纯副本；活的 git 仓库在 Desktop 下同名目录。
- ads-stats-2025 的 `slides/ notebooks/ datasets/ exercises/ notes/` 仍只有 `.gitkeep`；实际已写好的教学材料（Python 四讲方案、guides 系列、三份 CSV）都在 statistics-data-analysis。

**排查方法留档**：`git -C <repo> log --format="%h|%ad|%s" --date=format-local:"%Y-%m-%d %a %H:%M"` + 按「星期+时段」聚合（`sort | uniq -c`），可把提交时间还原成课表指纹。

## 落地：两份 README 写入班级信息（宏哥选「两个都写」）

在标题下各插入一张「课程 / 班级 / 上课时间 / 姊妹仓库」两列表格 + 一行口径说明：

| 文件 | 班级 | 上课时间 | 姊妹仓库指向 |
|---|---|---|---|
| `Desktop/ads-stats-2025/README.md` | 2025 级广告学 2 班 | 每周四下午 | statistics-data-analysis（1 班） |
| `repos/ads-stats-2025/README.md` | 同上（副本，保持一致） | 同上 | 同上 |
| `repos/statistics-data-analysis/README.md` | 2025 级广告学 1 班 | 每周三晚上 | ads-stats-2025（2 班） |

- 三份文件表格结构用 python 校验：均为 6 行 × 每行 3 根竖线（两列 GFM 表），`repos/` 与 `Desktop/` 两份 ads-stats-2025 的 README 逐字节一致。
- 只改 README，**未 commit / 未 push**：`Desktop/ads-stats-2025` 待提交 README；`repos/statistics-data-analysis` 工作区另有非本次改动（automation memory、llm-wiki-agent 子模块指针、未跟踪的 2026-09-17.md）。

## 任务：SSH agent 推送（受阻）→ 回落 HTTPS，并把 repos/ads-stats-2025 接上远端历史

**起因**：宏哥要求「下载 gh cli、用 ssh agent 方式推送本仓库、复用为 skill、测试」。

**gh CLI 无需下载**：已装 v2.98.0，登录 `yangjh-xbmu`（keyring），scopes = `gist, read:org, repo, workflow`。

### 结论一：这台机器当前**无法**用 SSH 推 GitHub（校园网整体封禁）

宏哥补充说明「校园网无法使用 ssh.github.com:443 的 SSH」，实测完整印证，并且比预期更彻底：

| 探测 | 结果 |
|---|---|
| `github.com:22` | 直连超时 |
| `ssh.github.com:443` 直连 | TCP 通，SSH 握手被 RST（`kex_exchange_identification: Connection reset`） |
| 会话代理 CONNECT `ssh.github.com:443` | 隧道 200 建立，**远端零字节后关闭** |
| 会话代理 CONNECT 到解析 IP `20.205.243.160:443` | 同样零字节 → **排除主机名过滤，是 IP 级封禁** |
| 11 个 GitHub 备用 IP × {22, 443} | 22 全超时、443 全 RST |
| 本机 19 个监听端口的 forward-proxy 能力扫描 | 无可用（9666/20101/18488/12900… 均非代理） |

→ 会话代理的出口**同样在校园网内**，靠它绕不过去。已把结论如实报告，宏哥选「先用 HTTPS 完成推送」。

### 结论二：SSH 侧的配置已全部就位（等网络放开即可用）

- `~/.ssh/gh-connect.py` —— 纯 stdlib 的 HTTP CONNECT 隧道，充当 `ProxyCommand`。本机**没有** `nc` / `connect.exe` / `socat`，只能自建。
- `~/.ssh/config` —— `Host github.com` → `HostName ssh.github.com` / `Port 443` / `ProxyCommand <python> gh-connect.py %h %p`。
- `~/.ssh/id_ed25519_github`（ed25519 无口令），指纹 `SHA256:n7zoMram3tjON8KGIeshEnMOFHxo3DPNzIcCn6f2878`。
- ssh-agent 已托管（会话级：`ssh-agent -s > ~/.ssh/agent.env`，之后 `. ~/.ssh/agent.env`）。

**公钥注册的两条路**（gh token 无 `admin:public_key`）：Deploy Key 走 `repo` scope 可全自动但仅限单仓库；账号 key 需 `gh auth refresh -s admin:public_key` 弹一次授权。**均未执行**（网络不通，注册了也没用）。

### 结论三：网络事实的**两处更正**（旧 skill 里的条目已不成立）

1. **「清代理直连」不再可用**：实测 `github.com:443` 直连 TCP CLOSED；`env -u *_proxy git fetch` 报 `Failed to connect ... after 21104 ms`。**必须走会话代理**。旧的"三级降级链"（会话代理 → 清代理直连 → 10808）已失效。
2. **会话代理是间歇性 502，不是永久失效**：`CONNECT tunnel failed, response 502` 会自己恢复；同一条命令里 `gh api user` 成功而 `git fetch` 报 502 是常态。**必须写重试循环**，单次失败不能判定断网。`ls-remote`（小请求）能过不代表 `fetch`（带 packfile）能过。
3. 另两条旧记录已被本次实测推翻（如实并列，未静默取舍）：`refs/remotes/origin/*` **现已落盘**（`for-each-ref` 有输出、`rev-parse origin/main` 可解析）；`git rev-parse --abbrev-ref '@{upstream}'` 无上游时**报 fatal**，不是返回字面量。

### 结论四：无 `.git` 副本接远端历史的正确接法（本次核心技法）

`repos/ads-stats-2025` 是活仓库的旧快照副本，远端已有 3 个提交。接法：

```bash
git init -b main
git remote add origin https://github.com/yangjh-xbmu/ads-stats-2025.git
git fetch origin main
git reset --mixed FETCH_HEAD     # HEAD 接上远端历史，工作区不动
git status --short               # 此时才是"真实差异"
```

**关键教训：`git status` 的 `M` 不等于"本地更新"。** 本次四个文件里有两个方向是反的：

| 文件 | 表面 | 真相 | 处置 |
|---|---|---|---|
| `scripts/01.ipynb` | ` M` | 本地把远端已改好的中文说明**回退**成 `print('hi ff')` | 丢弃本地 |
| `.workbuddy/memory/2026-09-17.md` | ` M` | 本地是旧版，比远端**少一整节**（gh-push 建 skill 记录） | 丢弃本地 |
| `.workbuddy/skills/gh-push/SKILL.md` | ` D` | 副本缺该文件（会被当删除提交） | `checkout` 恢复 |
| `README.md` / `MEMORY.md` | ` M` | 本地确实是新版 | 提交 |

只看 `status` 会误判两个文件、丢掉一整节记录。**每个非 `??` 文件都要 `git diff` 看方向。**

### 产出：gh-push skill 升级（两条通道）

`.workbuddy/skills/gh-push/SKILL.md` 重写：新增「通道 A/B 选择」、「方向性核对」（含本次实战案例表）、「无 `.git` 副本接远端历史」三节；把 SSH 隧道完整方案与封禁证据写入通道 B；更正上面三处失效事实；降级链改为重试循环。skill 保持**项目级**（沿用原位置，未另建同名 skill，避免重复）。

### 执行结果：推送成功（快进，未强推）

```
6667d6f..30622e8  main -> main
```

提交 `30622e8 docs: sync course README, 09-24 memory and gh-push skill upgrade`，5 文件 309 增 60 删。远程哈希与本地一致，工作区干净。`gh repo view` 交叉确认 `pushed=2026-09-24T08:54:00Z`。

**两条执行中才暴露的问题**（已补进 skill）：

1. **`push.autosetupremote=true` 未生效**：`git push origin main` 成功，但 `branch.main.merge` 仍为空 → 推完必须查 `git config --get branch.<b>.merge`，缺了用 `git branch --set-upstream-to=origin/main main` 补。本次已补上。
2. **验证命令本身会被 502 打断**：`git ls-remote` 取远程哈希时被代理断掉，返回空。**不能用它判定推送失败**——以 `git push` 自身输出（`<old>..<new>  main -> main`）+ `gh repo view --json pushedAt` 交叉确认。

**skill 自测**：写脚本校验两个项目级 skill 的 frontmatter（必填字段、name 与目录名一致、description 长度、代码围栏成对、必需章节）。`gh-push` 全项通过（description 288 字符、围栏 32 个成对、28 个标题）。`concept-learning-generator` 报"缺报告格式/边界章节"——**是自测脚本把 gh-push 的模板硬套上去的误报**，不是该 skill 的问题，脚本本身需按 skill 分别校验。

**SSH 预检复跑**：`ssh -T git@github.com` → `Connection closed by UNKNOWN port 65535`（隧道脚本自报 `tunnelling ssh.github.com:443 via proxy`，说明隧道建立正常、是出口封禁）。skill 第七节的断言成立。

