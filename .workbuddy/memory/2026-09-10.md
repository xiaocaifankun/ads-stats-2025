# 2026-09-10

## 复制 concept-learning-generator skill 到本项目 + 生成 Agent 记忆系统学习资料

**复制 skill**
- 源：`repos/concept-learning-materials/.workbuddy/skills/concept-learning-generator/SKILL.md`
- 目标：本项目 `.workbuddy/skills/concept-learning-generator/SKILL.md`（项目级，本次新建 `.workbuddy` 目录）
- 该 skill 定位：把任意「概念」转成 9 板块结构化 HTML 学习卡片（学习目标 / 核心问题 / 第一人称个人解释 / 核心机制 / 可视化讲解 / 应用场景 / 易混淆边界 / 自测题 / 参考来源），核心纪律是「先找真实来源再写自己的话，禁止伪造引用链接」。

**产出**
- `learning-materials/agent-memory.html`（新建 `learning-materials/` 目录，为该 skill 的默认输出路径约定）
- 单文件独立 HTML，内联全部 CSS，零外部依赖（无 script / 无 link / 无 img / 无 CDN），可离线双击打开。
- 8 个 `<section>`：01 学习目标 / 02 核心问题 / 03 个人解释 / 04 核心机制 / 05 应用场景 / 06 易混淆与边界 / 07 自测问题 / 08 参考来源。

**可视化讲解（3 张，全部纯 HTML+CSS）**
1. 记忆类型分类：工作记忆（易失）+ 语义/情景/程序记忆三个可持久化仓库。
2. 检索评分：`score = recency + importance + relevance`，附 recency 的 `0.99^Δt` 衰减条（0h=1.00 / 1h=0.99 / 24h=0.79 / 72h=0.48 / 168h=0.18，均经脚本实算核对）。上方三条彩色条为**示意**，已在图说中显式标注。
3. AgentCore Memory 长期记忆的异步三阶段管道：Extraction → Consolidation(ADD/UPDATE/NO-OP) → Reflection。

**自测题（8 题，答案全部 `<details>` 折叠）**
简答 4 题（概念辨析 / 核心机制 ×2 / 风险边界）+ 选择 2 题 + 应用 2 题，覆盖 skill 要求的四个维度。

**来源（12 条，全部 2026-09-10 逐条校验可访问）**
CoALA arXiv:2309.02427 · Generative Agents arXiv:2304.03442 · MemGPT arXiv:2310.08560 · Anthropic 上下文工程博客 · LangChain memory 文档 · Claude Academy Context Management · Claude Managed Agents memory 文档 · AWS 中国博客 AgentCore Memory · LangMem SDK · MCP 官方 memory server · HippoRAG arXiv:2404.16130 · Chroma Context Rot 报告。

**踩坑 / 环境**
- `https://learn.microsoft.com/en-us/azure/architecture/ai-ml/guide/agent-memory` 返回 404，已弃用，未采用。
- `agent-browser` 首次 `open` 直接 SIGTERM 退出：根因是 Chromium 从未安装（`~/AppData/Local/ms-playwright` 与 `~/.cache/ms-playwright` 均不存在）。安装需带代理 `HTTPS_PROXY=http://127.0.0.1:10808`。
- 外网链接校验统一走 `curl -x http://127.0.0.1:10808`。

## 第二轮：用同一 skill 生成《WorkBuddy 记忆系统》学习卡片

**产出**：`learning-materials/workbuddy-memory-system.html`（单文件、纯 HTML+CSS、零外部依赖）。

**与第一份的分工**：agent-memory.html 讲**通用 Agent 记忆理论**（CoALA / Generative Agents / MemGPT / AgentCore）；本份讲 **WorkBuddy 本机实现**，两份互相引用、不重复。

**内容要点**
- 三层记忆：① 云端记忆（只读，服务端画像）② 用户级 `~/.workbuddy/MEMORY.md`（跨项目、≤4000 字符）③ 工作区 `{workspace}/.workbuddy/memory/`（`YYYY-MM-DD.md` 追加日志 + `MEMORY.md` 项目笔记、≤3000 字符）。
- 关键机制：30 天蒸馏（旧日志先提炼进 MEMORY.md 再删除）；四类自动写入触发条件（建/改项目、修 Bug、告知偏好、技术选型）；两条历史检索路径（本地日志 vs 跨会话检索，后者零访问当前对话）。
- 新增识别的易混点：**记忆 vs 身份文件**（`~/.workbuddy/` 下 `SOUL.md`/`IDENTITY.md`/`USER.md` 定义 AI 是谁，与"记忆存事实"职责不同）。

**本机实测证据**：`~/.workbuddy/MEMORY.md`、`SOUL.md`、`USER.md`、`{workspace}/.workbuddy/memory/2026-09-10.md` 均确认存在。

**可视化**：2 张（三层记忆堆叠图、记忆生命周期流程图），纯 HTML+CSS，均带 `.viz-caption` 文字兜底。
**自测题**：7 题（简答 3 + 选择 2 + 应用 2），答案全部 `<details>` 折叠。
**来源**：4 条（均为 WorkBuddy 使用指南 / 实践笔记，非 arXiv 学术来源——因为讲的是具体产品实现）。

**验证**：agent-browser 打开 file:// 逐段截图核验（header / 图1 / 图2 / 自测 / 来源），排版正常；注意 `--full-page` 参数不被支持，需用 `eval` + `scrollIntoView` 分段截。

## 第三轮（重要返工）：禁用 ima/文库/百科，改为官方文档来源 + 优化 skill

**用户指令**：禁止使用 ima、各类文库、各类百科的内容，重新生产，并优化 skill。

**关键发现（推翻第二轮结论）**
- 第二轮那 4 条来源**全部是 `ima.qq.com` 的二手笔记聚合页**，内容与官方口径**冲突**，已全部弃用。
- 官方文档对「记忆」的定义与我第二轮写的**完全不同**：官网 <https://www.workbuddy.cn/docs/workbuddy/From-Beginner-to-Expert-Guide/Function-Description/Memory> 明确「记忆」是**从会话历史自动提取个人信息**（事实信息/偏好习惯/人物关系/近期跟进事项）形成的**记忆数据**，**每晚整理当天会话**，入口在**头像 → 设置 → 记忆**，用**对话式**编辑，支持查看/删除/关闭/从其他 AI 导入，不消耗积分、仅本人可见。
- 第二轮所谓「三层记忆 + 4000/3000 字符上限 + 30 天蒸馏」**全部来自 ima 二手文，官方文档无此说法**，已从卡片中全部删除。
- 官方文档里真实存在的「记忆」相关机制是三处：① 记忆功能（界面管理）；② 本地身份/工作记忆文件（`~/.workbuddy/` 下 `SOUL.md`/`USER.md`/`MEMORY.md` 等，文件管理）；③ **项目级记忆文件 `.codebuddy/CODEBUDDY.md`**（官方「项目」页原文，可提交版本控制，团队共享）。②与①的对应关系**官方未说明，卡片中明确标注不推断**。

**本机实测（用于区分「官方口径」与「本机形态」）**
- `~/.workbuddy/` 存在 `SOUL.md`(151B) / `IDENTITY.md` / `USER.md` / `MEMORY.md`(914B) / `BOOTSTRAP.md` / `user-state.json`。
- `~/.workbuddy/memory/` 含 `655fbc57-...-a91a-c1f0f284d8a8_memory.md`（13.7KB，标题「User Memory Profile」，带 `Version: 129` 与 `Last updated` 时间戳）+ `.bak`。**与官方「每晚整理、定期更新」的记忆数据形态吻合**。
- `{workspace}/.workbuddy/memory/` 只有 `2026-09-10.md`，**无 `MEMORY.md`**（第二轮误称存在，已修正）。
- 项目 `.codebuddy/` 目录**不存在**，repos 下各仓库也均无。

**Skill 优化**（`.workbuddy/skills/concept-learning-generator/SKILL.md`）
- Step 1 新增**来源红线**：明令禁止采信文库类（百度文库/豆丁/道客巴巴）、百科类（百度百科/维基/MBA智库）、AI 知识库分享页（`ima.qq.com` 及其 wiki 页/Notion/语雀/飞书公开页）、内容农场与 CSDN 洗稿文；要求检索时叠加 `site:docs.xxx.com` 等限定，并**优先抓取官方文档站导航目录**定位页面。
- 新增**「抓取而非回忆」**（来源必须实际抓取后写入）与**「本地证据核对」**（涉及本机实物时须实测，且**明确区分官方描述 vs 本机形态**，不得混为一谈）。
- 第五节「资料来源要求」新增「禁止采信二手聚合与百科类内容」与「本地证据优先」两条。
- 第六节自检清单新增两项：来源是否全为官方/一手、是否已用本机实测核对并区分口径。

**产出**：`learning-materials/workbuddy-memory-system.html` 已重写（8 section / 2 图 / 6 details / 19 处 `[来源 N]`）；来源 4 条**全部为官方**（Memory 页、Project 页、Quickstart、腾讯隐私政策），均 curl 200；文中显式声明「未采用任何文库、百科或第三方笔记聚合内容」。

**环境坑（重要，可复用）**
- `agent-browser open` 报 **SIGTERM / exit 1 且无输出**的真因：`~/.agent-browser/default.pid` 残留**僵尸 pid 文件**（pid 已被 bash 复用）。
- 另实测：`agent-browser --debug` 反而能正常打开——后续遇到静默 SIGTERM，先 `rm -f ~/.agent-browser/default.pid`，重试仍不行再加 `--debug`。
- 验证脚本应**链式单条命令**执行（`open ... && sleep && eval ... && screenshot`），因为守护进程在命令之间可能掉线，分多次调用会 `eval` 静默失败（截图仍停在原位，易误判为「渲染正常」）。

## 第四轮：在 ads-stats-2025 建立 LLM Wiki 个人知识库

**需求**：在当前目录按 Karpathy LLM Wiki 模式建个人知识库，先查 GitHub 项目再照做。

**查证**（先检索后动手，未凭记忆）
- 原始出处：Karpathy Gist `442a6bf555914893e9891c11519de94f`（2026-04-04），已 WebFetch 取回原文。
- 参考实现三家：`DavidPFussell/llm-wiki`（简洁 starter，本库 AGENTS.md 主要参照）、`nvk/llm-wiki`（多智能体编排，最全）、`jnystrom14/llm-wiki`（完整 Python 工具链）。
- 已 WebFetch 拿到 DavidPFussell 的 AGENTS.md 全文，据其组织规则。
- 核心概念：三层（raw / wiki / schema）、三操作（ingest / query / lint）、两导航文件（index.md 内容索引 + log.md 追加日志）。

**产出**（新建，与已有广告学课件内容互不干扰）
- `AGENTS.md` — schema / 操作手册（中文；含启动例程、不可违背规则、页面类型、命名、frontmatter、链接、矛盾处理、index/log 规则、三大 workflow、归档策略、人机分工）
- `raw/{sources,assets}/`、`wiki/{sources,entities,concepts,outputs,maintenance,indexes}/`
- `wiki/index.md`（索引）、`wiki/log.md`（追加日志，已记初始化条目）、`wiki/overview.md`、`wiki/conventions.md`（预填用户偏好：结论先行/表格优先/极简无 emoji/类比讲解/中文作答）
- `templates/{source,entity,concept,output}.md` 四类页面模板
- `scripts/llm-wiki.py` + `scripts/llm-wiki`（本地 lint/stats 工具，无需 LLM）
- `WIKI-README.md`（独立于课程 README，避免覆盖）、`docs/llm-wiki-pattern.md`

**避坑**：原仓库 `README.md` 是课程文档，故知识库说明另起 `WIKI-README.md`，未动原文件。

**验证**：`python scripts/llm-wiki.py lint` 输出 0 问题、exit 0；`stats` 正常。

## 第五轮（重要纠正）：用户指出「为什么不下载那个 GitHub 仓库」

**用户不满**：第四轮我只"照模式自建"，没有下载用户指定的 GitHub 项目。用户要的是**照那个项目来**，不是照模式仿。

**查证定位**（关键：仓库名要对上）
- 用户说的 "LLM wiki Agent" = **`SamurAIGPT/llm-wiki-agent`**（3484 stars，MIT，Python）。
- 另有同名 `fcwu/llm-wiki-agent`（Claude-only 简化版，含 `setup.sh`），非主目标。

**处理**（按用户选择：官方仓库完全替换 + 断掉 git 关联）
- `git clone --depth 1 https://github.com/SamurAIGPT/llm-wiki-agent.git` 到临时目录 `.llm-wiki-agent/`（需代理 `https_proxy=http://127.0.0.1:10808`）。
- 第四轮自建的那套（`wiki/`、`AGENTS.md`、`templates/`、`scripts/`、`docs/`、`WIKI-README.md`）**先备份**到 `.workbuddy/backup-llm-wiki-custom/`，再删除。
- 删除 `.llm-wiki-agent/.git`（断开关联），用 `shopt -s dotglob && cp -r ./* ../` 整体搬到仓库根目录，删除临时目录。

**官方仓库关键构成（与自制版的差异）**
- schema 三份：`CLAUDE.md`（Claude Code）、`AGENTS.md`（Codex/OpenCode）、`GEMINI.md`（Gemini CLI）。
- `.claude/commands/`：4 个斜杠命令 `wiki-ingest` / `wiki-query` / `wiki-lint` / `wiki-graph`。
- `tools/` 11 个 Python 脚本：`health.py`（零 LLM 结构检查）、`lint.py`（语义检查）、`build_graph.py`（vis.js 图谱 + Louvain 社群检测）、`file_to_md.py` / `pdf2md.py`（markitdown 多格式转换）、`ingest.py` / `query.py` / `heal.py` / `refresh.py`。
- 命名硬规则：实体/概念页 **`TitleCase.md`**（如 `OpenAI.md`），来源页 `kebab-case.md`。
- 目录语义：查询产出在 `wiki/syntheses/`（不是 outputs）；操作类型含 `health` / `graph`。
- `private/` 目录始终 gitignore（敏感内容安全区）。

**踩坑**
- 复制时同名文件覆盖：官方 `README.md` 覆盖了课程 `README.md`（**已从 `git show HEAD:README.md` 恢复**；官方说明改名 `LLM-WIKI-README.md` 保留）；官方 `.gitignore` 覆盖了项目原 `.gitignore`（已手工合并补回 `private/`、Obsidian、生成物等规则）。
- `tools/build_graph.py` 缺 `networkx`（社群检测）与 `litellm`（语义边推断），仅影响图谱增强；`build_graph.py` 会尝试调用 LLM 推断隐式关系——注意可能产生 API 调用。

**验证**：`tools/health.py` 全绿（0 空页 / 0 索引不同步 / 0 日志缺失），exit 0。

**教训**：用户明确点名某个 GitHub 项目时，必须**先找到并克隆那个仓库**，再谈落地方式；不要用"理解其模式后自行仿建"替代"照该项目执行"。

## 第六轮：学习 4 份 Agent 主题手册 + 装载知识库 schema

**用户指令**：学习 `repos/statistics-data-analysis/` 下 4 份 HTML 手册，并进入当前项目（ads-stats-2025）的知识库。

**4 份手册已完整读取并结构化吸收**（注意：`statistics-data-analysis` 与 `ads-stats-2025` 是两个不同仓库目录）

| 手册 | 文件 | 结构 | 溯源主线 |
|---|---|---|---|
| Agent Skills | `agent-skills-guide.html` (359 行) | 它是什么 / 渐进式披露 / 一次任务全程 / 五条铁律 / 溯源 / 自测 | Anthropic 工程博客、agentskills.io、platform.claude.com、anthropics/skills |
| Agent 智能体 | `agent-guide.html` (547 行) | 它是什么 / 核心设计 / 运行全程 / 铁律 / 溯源 / 自测 | AIMA 教材、Shoham 1993、Wooldridge & Jennings 1995、ReAct arXiv:2210.03629、Building Effective Agents |
| 通用 Agent 记忆系统 | `agent-memory-guide.html` (738 行) | ①它是什么 ②核心设计（记忆分类学）③运行全程（Retrieve/Record 回路 + 上下文工程三策略）④铁律 ⑤WorkBuddy 三层记忆 ⑥溯源 ⑦自测 | Anthropic Context Engineering、Building Effective Agents、Mastra agent-memory、Anthropic Managed Agents 2026、Mem0 |
| Agent 记忆系统 | `agent-memory-system-guide.html` (564 行) | ①它是什么 ②三大通用范式 ③四步循环 ④WorkBuddy 记忆设计 ⑤铁律 ⑥溯源 ⑦自测 | Generative Agents arXiv:2304.03442、MemGPT arXiv:2310.08560、Anthropic Memory Tool、Context Engineering Cookbook、WorkBuddy 官方文档 |

**两版记忆手册的分工**：`agent-memory-guide.html` 走**记忆分类学**路线（工作/语义/情景/程序/检索/参数/前瞻 七类 + 读写回路时序 + 上下文工程三策略）；`agent-memory-system-guide.html` 走**范式路线**（记忆流三因子打分 / MemGPT OS 式分层换页 / Anthropic 文件即记忆）。两者对 WorkBuddy 三层记忆的描述一致，属互补而非重复。

**知识库装载状态**（`ads-stats-2025`，即 `SamurAIGPT/llm-wiki-agent` 形态）
- 已装载 schema：`AGENTS.md` 全文重读确认 —— 三层目录（`raw/` 不可变 / `wiki/` agent 独占 / schema 文件）、5 个 trigger（ingest / query / health / lint / build graph）、4 类页面 type、命名硬规则（来源 `kebab-case.md`，实体与概念 `TitleCase.md`）、health（零 LLM）vs lint（有 LLM）边界、Graph Workflow 两遍构建、Phase 3 自动链接硬规则（HG-WA-01 禁止从断链自动建页 / HG-WA-02 斜杠命令不得重复覆盖）。
- `tools/health.py` 实跑**全绿**：Scanned 1 page / 0 空页 / 0 索引不同步 / 0 日志缺失。
- **wiki 当前为空库**：`wiki/` 下仅 `index.md`（只 Overview 条目）、`log.md`（setup + graph 两条）、`overview.md`（"No sources ingested yet"）；`wiki/sources|entities|concepts|syntheses` **四个子目录尚不存在**（需首次 ingest 时创建）。
- `raw/sources/` 与 `raw/assets/` 仅有 `.gitkeep`，无任何源文档。
- `graph/graph.json` 仅 1 node（overview）/ 0 edges。

**待办（用户未下令，等指令）**：是否将上述 4 份手册 ingest 为知识库来源页；`BOOTSTRAP.md` 身份确认仍未完成（`IDENTITY.md` 的 Creature/Vibe/Emoji 与 `USER.md` 的 City/Notes/Context 均为空）。

